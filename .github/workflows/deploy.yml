name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main # Chỉ chạy khi bạn push lên nhánh chính (main)

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4
      
      # Thiết lập môi trường Node.js (cần thiết cho Vite và npm/pnpm)
      - name: Setup Node.js environment
        uses: actions/setup-node@v4
        with:
          node-version: '20' # Sử dụng phiên bản Node.js 20

      # Cài đặt và Build dự án
      - name: Install dependencies 📦
        run: npm install # Nếu bạn dùng npm
        # Nếu bạn dùng pnpm, thay thế bằng: run: pnpm install --frozen-lockfile
      
      - name: Build project 🏗️
        run: npm run build # Chạy lệnh build của Vite

      # Triển khai thư mục 'dist' lên GitHub Pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist # Thư mục chứa các tệp tĩnh sau khi build
